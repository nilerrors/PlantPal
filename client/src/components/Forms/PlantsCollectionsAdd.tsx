import { PlantsCollection } from '../../types'

type Props = {
  addToCollection: (collection: PlantsCollection) => void
}

export function PlantsCollectionsAdd({ addToCollection }: Props) {
  return <>PlantsCollectionsAdd</>
}
