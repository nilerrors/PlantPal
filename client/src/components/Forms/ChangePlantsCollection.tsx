import { PlantsCollection } from '../../types'

type Props = {
  collection: PlantsCollection
  setFormClose: () => void
}

export function ChangePlantsCollection({ collection, setFormClose }: Props) {
  return <>ChangePlantsCollection</>
}
